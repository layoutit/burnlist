/** Deterministic allocation and input ceilings for the terminal viewer. */
export const TERMINAL_RESOURCE_LIMITS = Object.freeze({
  httpJsonBytes: 2 * 1024 * 1024,
  definitionBytes: 512 * 1024,
  sseFrameBytes: 64 * 1024,
  sseRemainderBytes: 128 * 1024,
  pendingInvalidations: 32,
  irNodes: 512,
  irDepth: 32,
  irStringBytes: 8 * 1024,
  irTextBytes: 128 * 1024,
  controls: 64,
  collections: 32,
  collectionPageSize: 100,
  collectionItems: 500,
  expandedKeys: 64,
  payloadNodes: 20_000,
  payloadDepth: 48,
  payloadStringBytes: 32 * 1024,
  payloadTextBytes: 1024 * 1024,
  terminalCells: 240 * 80,
  imageCells: 160 * 50,
  streamingCards: 200,
  streamingCardTextBytes: 32 * 1024,
  pngCompressedBytes: 2 * 1024 * 1024,
  pngChunks: 128,
  pngDimension: 4096,
  pngRgbaBytes: 16 * 1024 * 1024,
  polygons: 2_000,
  animations: 1,
  animationFps: 20,
  frameWorkCells: 240 * 80,
} as const);

export type TerminalResourceLimit = keyof typeof TERMINAL_RESOURCE_LIMITS;
